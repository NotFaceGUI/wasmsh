[test]
name = "readlink-path"
tags = ["utility", "P0"]
tier = "P0"
requires = ["readlink", "ln"]

[setup]
files = { "/some/path" = "data" }

[input]
script = "ln -s /some/path /link\nreadlink /link"

[expect]
status = 0
stdout = "/some/path\n"
