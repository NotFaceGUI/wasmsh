[test]
name = "command-substitution-assignment-or-fallback"
tags = ["expansion", "command-substitution", "differential", "P0"]
tier = "P0"
class = "compat-bash"
stage = "differential"
requires = ["command-substitution", "variable-assignment", "echo"]

[input]
script = "x=$(false) || rc=$?; echo rc=$rc"

[expect]
status = 0
stdout = "rc=1\n"

[oracle]
compare = true
shells = ["bash"]
