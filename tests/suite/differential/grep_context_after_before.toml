[test]
name = "grep-after-before-context"
tags = ["utility", "grep", "differential", "P0"]
tier = "P0"
class = "compat-bash"
stage = "differential"
requires = ["grep", "printf"]

[input]
script = "printf 'a\nb\nMATCH\nc\nd\ne\n' | grep -A2 -B1 MATCH"

[expect]
status = 0
stdout = "b\nMATCH\nc\nd\n"

[oracle]
compare = true
shells = ["bash"]
