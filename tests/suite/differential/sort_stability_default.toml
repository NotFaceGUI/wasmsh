[test]
name = "sort-stability-default-tiebreak"
tags = ["utility", "sort", "differential", "P1"]
tier = "P1"
class = "compat-bash"
stage = "differential"
requires = ["sort", "printf"]

[input]
script = "printf 'b 1\na 1\nb 0\na 0\n' | sort -k2"

[expect]
status = 0
stdout = "a 0\nb 0\na 1\nb 1\n"

[oracle]
compare = true
shells = ["bash"]
