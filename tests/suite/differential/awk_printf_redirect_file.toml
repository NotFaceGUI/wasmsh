[test]
name = "awk-printf-redirect-file"
tags = ["awk", "redirection", "differential", "P0"]
tier = "P0"
class = "compat-bash"
stage = "differential"
requires = ["awk", "cat"]

[input]
script = "awk 'BEGIN{printf \"%s\\n\", \"hi\" > \"out.txt\"}'\ncat out.txt"

[expect]
status = 0
stdout = "hi\n"

[oracle]
compare = true
shells = ["bash"]
