[test]
name = "awk-printf-char-from-code"
tags = ["awk", "printf", "differential", "P1"]
tier = "P1"
class = "compat-bash"
stage = "differential"
requires = ["awk"]

[input]
script = "awk 'BEGIN{printf \"%c\\n\",65}'"

[expect]
status = 0
stdout = "A\n"

[oracle]
compare = true
shells = ["bash"]
