[test]
name = "grep-combined-context-flag"
tags = ["utility", "grep", "differential", "P1"]
tier = "P1"
class = "compat-bash"
stage = "differential"
requires = ["grep", "printf"]

[input]
script = "printf 'a\nb\nMATCH\nc\nd\n' | grep -C1 MATCH"

[expect]
status = 0
stdout = "b\nMATCH\nc\n"

[oracle]
compare = true
shells = ["bash"]
