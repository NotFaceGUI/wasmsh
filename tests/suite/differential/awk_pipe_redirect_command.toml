[test]
name = "awk-pipe-redirect-command"
tags = ["awk", "redirection", "differential", "P1"]
tier = "P1"
class = "compat-bash"
stage = "differential"
requires = ["awk", "cat"]

[input]
script = "awk 'BEGIN{print \"hello\" | \"cat\"}'"

[expect]
status = 0
stdout = "hello\n"

[oracle]
compare = true
shells = ["bash"]
